pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }
        stage('Build') {
            steps {
                sh 'npm install'
                sh 'npm run build'
                echo 'Building..right now'
            }
        }
        stage('Test') {
            steps {
                echo 'Testing..now'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....now'
            }
        }
    }
}