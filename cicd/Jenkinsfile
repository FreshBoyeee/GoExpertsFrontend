pipeline {
    agent any
    stages {
        stage('Build') { 
            steps {
                sh 'npm install -g n'
                sh ' n 12'
                sh 'npm -v'
                sh 'node -v'
                sh 'npm install'
                sh 'npm run build'
                echo "building..."
            }
        }
    }
}