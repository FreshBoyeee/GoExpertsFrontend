pipeline {
    agent any
    stages {
        stage('Build') { 
            steps {
                // sh 'npm install' 
                // sh 'npm run build'\
                // sh 'npm install --verbose'
                sh 'npm -v'
                echo "building..."
            }
        }
    }
}