pipeline {
    agent any
    stages {
        stage('Build') { 
            steps {
                sh 'npm -v'
                sh 'node -v'
                sh 'npm run build'
                echo "building..."
            }
        }
    }
}