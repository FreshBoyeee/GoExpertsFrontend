pipeline {
    agent any
    stages {
        stage('Build') { 
            steps {
                sh 'sudo apt update'
                sh 'sudo apt install nodejs npm -y'

                sh 'npm -v'
                sh 'node -v'
                // sh 'npm run build'
                echo "building..."
            }
        }
    }
}