pipeline {
    agent any
    stages {
        stage('Build') { 
            steps {
                sh 'npm install'
                sh 'npm run build'
                sh 'ls'
                sh 'cat /var/jenkins/workspace/goExpert/build/index.html'
                sh 'pwd'
                sh 'ls'
            }
        }
    }
}