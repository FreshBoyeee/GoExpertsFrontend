pipeline {
    agent any
    stages {
        stage('Build') { 
            steps {
                // sh 'npm install'
                // sh 'npm run build'
                sh 'aws s3 mb s3://asdgsdhsgsss'
                sh 'aws s3 ls'
            }
        }
    }
}