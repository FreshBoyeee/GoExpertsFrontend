pipeline {
    agent any
    stages {
        stage('Build') { 
            steps {
                sh 'npm install'
                sh 'npm run build'
                sh 'ls'
                sh 'cd /var/jenkins/workspace/goExpert/build'
                sh 'pwd'
                sh 'ls'
            }
        }
    }
}